numpy>=1.24
pandas>=2.0
torch>=2.1
pyarrow>=14.0
s3fs>=2024.0

# `pip install -r requirements.txt` installs the CPU-only build of torch.
# This code is GPU-only -- install a CUDA-matched torch build instead:
#   pip uninstall torch -y
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Check your driver's supported CUDA version first with `nvidia-smi`.
#
# s3fs is required for reading/writing s3:// paths directly via pandas
# (read_parquet/to_parquet with storage_options={}). AWS credentials are
# NOT configured here -- s3fs picks them up automatically from whatever
# is already available in the environment (an IAM role attached to the
# instance, ~/.aws/credentials, or AWS_* environment variables). Verified
# end-to-end against a real S3-API-compatible server before shipping --
# see README.md.
